gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Exception: temporary until the real CloudShell-generated keystore is
# in place (blocked on CloudShell access — see ci-cd.md, "Android
# signing"). Checked in with a fixed, non-secret password so every CI
# build shares one identity, matching exercise-app's same workaround.
!app/sideload.keystore.jks
